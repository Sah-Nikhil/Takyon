{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The Palette. Deliberately narrow: it shows an input row and dismisses itself.",
  "windows": ["palette"],
  "permissions": ["core:default"]
}
